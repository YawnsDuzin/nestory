[
## 12. 갱신 (코드 업데이트)

```bash
cd /home/dzp/dzp-main/program/nestory
git pull origin main
uv sync                                  # 의존성 변경 시 반영
uv run alembic upgrade head              # 새 마이그레이션 적용
sudo systemctl restart nestory-app nestory-worker   # systemd 사용 시
```

## 윈도우
uv sync                                  # 의존성 변경 시 반영

# 새 마이그레이션 적용
uv run alembic upgrade head

# 사전 조건: alembic upgrade head 가 끝나서 regions 테이블이 존재해야 함
uv run python -m scripts.seed_regions

# 1. 양평 데모 데이터
uv run python -m scripts.seed_yangpyeong_demo

uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

uv run python -m app.workers.runner

====================

첨부이미지와 같이 상단 메뉴의 "피드", "탐색", "시군 매칭", "검색" 에서 "시군 매칭", "검색"에만 이모티콘이 있어서 일관성이 없는 것 같은데.. 표시형식 개선안을 제안해줘. sns 서비스들의 ui/ux를 확인해서 와이어프레임 형식으로 개선 안을 제안해줘.

[추가1] 모바일모드에서는 첨부이미지와 같이 "피드", "탐색", "시군 매칭" 메뉴가 안보이는데,, sns 서비스에서 일반적인 ux인지 검토해줘.
[추가2] 제안헤준 하단 탭바 컴포넌트 방식으로 작업 진행해줘.

====================

첨부이미지와 같이 우측아래에 녹색원이 계속 표시되는데,, 일반적인 ux인지 확인해줘.

[추가1] 권장 수정안으로 수정작업 진행해줘.

====================

입력화면이 첨부이미지와 같이 프로필 사진만큼 입력 부분이 우측으로 정렬되어 있고, 입력 하는 부분이 분리가 안되어 있는 느낌이야.. sns 서비스의 입력 화면을 참고해서 개선안을 제안해줘.

[추가1] 입력항목의 적당한 좌우 여백을 추가해줘.

====================

입력하는 페이지의 입력항목에 적당한 좌우 여백을 추가해줘.

====================

첨부이미지의 프로필 편집 창의 구성이 자연스럽지가 않아 보이는데,  sns 서비스들의 ui/ux 참고해서 개선안을 제안해줘.

[추가1] 추천 제안으로 개선해줘.

====================

작성된 글 수정 기능이 있는지 확인해서 알려줘.

[추가1] sns 들의 ui/ux를 확인해서 작업 안을 제안해줘.
[추가2] "1. 옵션 A · P1.6a부터 — 권장. sub-plan 작성부터 시작" 방안으로 작업 진행해줘.

[추가3] "1. Subagent-Driven (권장)" 방법으로 진행해줘.
[추가3-1] 테스트를 어떻게 진행해야 하는거야??? docker는 지금 사용안하고 있어.
[추가3-2] 수정 가능하도록 개선된거야??
[추가3-3] 오늘 pc에는 docker 테스트 안되니깐.. 제외하고 추가진행 사항 제안해줘.

====================

/commit

====================
====================
====================

AI 기능을 사용하는 방안 검토 및 제안

====================

마케팅 아이디어..
동일한 주제의 카페에서 홍보는 제재를 먹을 수 있으므로,,

비슷한 주체의 카페나 사이트에 문의와 같은 느낌으로 홍보
 - 예) 캠핑카페, 1인여행, 한달살이카페 등..
홍보 자동화
- 자체 유튜브/쇼츠 생성
- 블로그 운영

경품 추첨??

====================]