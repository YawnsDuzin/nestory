[
## 12. 갱신 (코드 업데이트)

```bash
cd /home/dzp/dzp-main/program/nestory
git pull origin main
uv sync                                  # 의존성 변경 시 반영
uv run alembic upgrade head              # 새 마이그레이션 적용
sudo systemctl restart nestory-app nestory-worker   # systemd 사용 시
```

## 윈도우
uv sync                                  # 의존성 변경 시 반영

# 새 마이그레이션 적용
uv run alembic upgrade head

# 사전 조건: alembic upgrade head 가 끝나서 regions 테이블이 존재해야 함
uv run python -m scripts.seed_regions

# 1. 양평 데모 데이터
uv run python -m scripts.seed_yangpyeong_demo

uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

uv run python -m app.workers.runner

====================

현재 한번 인증하면, 계속 로그인 상태로 유지되는데,, 일반적인 로직인지 검토해줘.

[추가1] "1. 비밀번호 변경 시 세션 무효화 안 됨 — app/routers/me.py:298" 부분은 개선해줘.

====================

아래와 같은 카피가 접속 사용자의 기준을 제한하지 않을지 검토해줘.

1년차에 좋다는 후기는 많아도,
3년차의 진실은 어디서 듣나요?

[추가1] 카피를 아래와 같이 변경해줘.
"1년차의 설렘도 진짜고, 3년차의 깊이도 진짜입니다 — 둘 다 들을 수 있는 곳"

[추가2] 아래의 문구는 그대로 가는게 좋을지 검토해줘.
    같은 사람의 1년차 → 3년차 회고로
    가짜 후기 없이 정착의 진실을 봅니다.
[추가2-1] 권장 안으로 수정작업 진행해줘.

====================

docker compose 는 사용안할거야.. 제거해줘. 테스트 시에도 아래의 스크립트로 실행 및 테스트해줘.
앞으로 모든 테스트 및 실행 이렇게 하도록 CLAUDE.MD 등 문서 수정해줘.

uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
uv run python -m app.workers.runner

====================
====================
====================
====================
====================

AI 기능을 사용하는 방안 검토 및 제안

====================

마케팅 아이디어..
동일한 주제의 카페에서 홍보는 제재를 먹을 수 있으므로,,

비슷한 주체의 카페나 사이트에 문의와 같은 느낌으로 홍보
 - 예) 캠핑카페, 1인여행, 한달살이카페 등..
홍보 자동화
- 자체 유튜브/쇼츠 생성
- 블로그 운영

경품 추첨??

====================