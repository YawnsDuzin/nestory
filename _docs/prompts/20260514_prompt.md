[
## 12. 갱신 (코드 업데이트)

```bash
cd /home/dzp/dzp-main/program/nestory
git pull origin main
uv sync                                  # 의존성 변경 시 반영
uv run alembic upgrade head              # 새 마이그레이션 적용
sudo systemctl restart nestory-app nestory-worker   # systemd 사용 시
```

## 윈도우
uv sync                                  # 의존성 변경 시 반영

# 새 마이그레이션 적용
uv run alembic upgrade head

# 사전 조건: alembic upgrade head 가 끝나서 regions 테이블이 존재해야 함
uv run python -m scripts.seed_regions

# 1. 양평 데모 데이터
uv run python -m scripts.seed_yangpyeong_demo

uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

uv run python -m app.workers.runner

====================

/brainstorming  첨부이미지인 로그인 후, 메인 페이지의 구성을 좀더 비주얼적으로 개선되고, 사용자 친화적으로 구성하는 개선안을 제안해줘.

[추가1] "④ 4개 다 + 신규 로고(image.png A안 또는 B안 선택) 반영" 으로 작업 진행해줘.
[추가2] 테스트는 docker 말고, 니가 playwright mcp 사용해서 직접 진행해줘.
[추가2-1] docker desktop 쓰지말라고
[추가2-2] uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000 로 띄워서 테스트해

====================

첨부이미지의 글내용 표시 카드의 구조를 와이어프레임으로 변경해서 알려줘

[추가1] 내용 표시 부분이이 헤더의 avatar 만큼 왼쪽 여백이 있어서, 오른쪽으로 정렬되어 부자연스럽게 보이고 있어. 양쪽에 적당한 여백을 추가하고 가운데 정렬되도록 수정해줘. 모든 카드 디자인에 동일하게 적용해줘.

====================

현재 "후기작성", "Journey 시작", "질문하기", "정착계획" 항목의 입력 폼이 첨부이미지와 같이 표시되고 있어.. 일관성이 없는것 같다.. 다른 비슷한 sns 서비스의 입력 ui/ux들을 검토해서 사용자 친화적이고 일관성 있는 방안을 제안해줘.

[추가1]
후속 제안해준 항목들의 항목 별 좀더 자세한 설명을 해줘. 잘 이해가 안되
    char counter (작은 회색 카운터, 하단 좌측 — title 300자/body 권장한도)
    draft 자동 저장 (localStorage)
    이미지 첨부 inline thumb 미리보기 (현재 markdown만 삽입)
    Cmd/Ctrl+Enter 단축키 submit
    태그 chip 입력 UI (Question — Enter로 chip 생성)
    region picker chip형 트리거 (시군 늘어나면 검색 모달)
[추가2] 제안해준 우선순위대로 작업 진행해줘.
[추가3] ① 커밋 + dev push — 지금까지 작업분(home uplift + post_card 정렬 + 쓰기 폼 통합 + 5개 개선) 한 번에 커밋
[추가4] .gitignore 정리 (qa-*.png 등 untracked 잡음 제거) 작업 진행해줘.

[추가5] 제안해준 "다음 영역 작업 (댓글 작성 / 상세 페이지 / 알림 / 검색 / 매칭 wizard 등)" 작업부분 정리해서 다시 안내해줘.
[추가5-1] 서비스 중인 sns 서비스의 ui/ux를 참고해서 제안해줘.

[추가6] "① Phase 1 풀패키지 (~5-6h, 가장 추천)" 로 작업 진행해줘.

[추가7] 답글에 답글 달려고 하면, 첨부이미지와 같이 입력항목이 가려서 잘 안보여.
[추가8] 계획한 작업 모두 완료된거야?

====================

첫번째 첨부이미지의 모바일 기본페이지에서 두번째 첨부이미지의 글씨가 단락?? 이 정리가 안되어 표시가 되고 있어. 검토해줘.

[추가1] "A. 첫 단락만 노출" 로 수정하면 어떻게 표시되는지 알려줘.
[추가1-1] "A. 첫 단락만 노출" 로 진행하는데,, 후기를 여러개(3개정도)  표시하는 방법은 어떤지 검토해줘.
[추가2] "권장 — B2 캐러셀" 방법으로 작업 진행해줘.

[추가3] 1. Playwright MCP로 실제 UI 확인 (메모리 권장 경로) — uv run uvicorn ... --reload --port 8000 띄워두면 브라우저로 hero 캐러셀 동작·접근성 확인 가능
[추가3-1] "1. 사용자가 직접 기존 uvicorn 터미널에서 Ctrl+C → 재실행"

[추가4] 첨부이미지와 같이 "캐러셀" 이 다음 누르면,, 잘려서 표시되고 있어.
[추가4-1] "1. 사용자가 직접 실행" 작업 완료했어

====================

실제 모바일에서 첨부이미지와 같이 표시되고 있는데,, 좀더 단락구분이나 정렬되서 표시되도록 개선해줘.

====================
====================
====================
====================

로그인 후, 메인페이지 ui/ux 개선

====================

입력화면 개선 (이미지 첨부 등)

====================

AI 기능을 사용하는 방안 검토 및 제안

====================

마케팅 아이디어..
동일한 주제의 카페에서 홍보는 제재를 먹을 수 있으므로,,

비슷한 주체의 카페나 사이트에 문의와 같은 느낌으로 홍보
 - 예) 캠핑카페, 1인여행, 한달살이카페 등..
홍보 자동화
- 자체 유튜브/쇼츠 생성
- 블로그 운영

경품 추첨??

====================]